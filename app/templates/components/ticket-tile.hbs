<ul>
  {{#each tickets as |ticket|}}
    <li>{{ticket.name}}<br>{{ticket.location}}<br>{{ticket.description}}<br>{{ticket.timestamp}}</li>
    <button {{action 'destroyTicket' ticket}}>Remove Ticket</button>
    <button {{action 'ticketInactive' ticket}}>Remove Ticket</button>
  {{/each}}

</ul>
